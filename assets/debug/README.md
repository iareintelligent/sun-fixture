# `assets/debug/`

Troubleshooting assets used during dashboard development:

- `debug-red.png` — solid red PNG for confirming a Picture Elements layer is wired correctly when masks aren't rendering.
- Other test patterns, alignment grids, and one-off diagnostic images.

Anything that ends up here is fair game to delete once the related issue is resolved — these aren't deliverables.
