# `config/themes/`

Custom Home Assistant theme YAML. Populated only if the floorplan dashboard's visual direction calls for theme-level color overrides — e.g., to set the Lovelace background to match the dark base palette of ADR-0001.

Possibly empty for the pilot floor; populate when the style guide solidifies.
